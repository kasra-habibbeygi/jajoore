import styled from '@emotion/styled';

export const TooltipStyles = styled.p({
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',

    img: {
        width: '25px',
        height: 'auto',
        filter: 'invert(50%) sepia(9%) saturate(1580%) hue-rotate(198deg) brightness(98%) contrast(91%)'
    }
});
