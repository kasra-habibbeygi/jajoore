import styled from '@emotion/styled';

export const MainField = styled.div({
    textarea: {
        borderRadius: '8px',
        color: '#666666',
        padding: '10px',
        height: '120px',
        display: 'flex',
        alignItems: 'center'
    }
});
