import styled from '@emotion/styled';

export const MainField = styled.div({
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',

    button: {
        fontWeight: '300'
    }
});
