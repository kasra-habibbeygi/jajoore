import React from 'react';

// Component
import ProfileLayoutProvider from '@/components/layout/profile-layout-provider';

const Turnover = () => {
    return <ProfileLayoutProvider>hi</ProfileLayoutProvider>;
};

export default Turnover;
